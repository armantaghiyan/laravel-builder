<?php

namespace App\Core\Application\Actions\{model};

use App\Core\Domain\{model}\Models\{model};
use App\Core\Domain\{model}\Repositories\{model}Repository;
use App\Http\Data\Admin\{model}\{model}UpdateData;
use Illuminate\Database\Eloquent\Model;


readonly class {model}UpdateAction {

    public function __construct(
        private {model}Repository ${modelCamel}Repository,
    ) {
    }

    public function execute({model}UpdateData $data, int $id): Model {
        $item = $this->{modelCamel}Repository->findOrErrorById($id);
        $this->{modelCamel}Repository->update($item, [
            {items}
        ]);

        return $item;
    }
}
